
<div class="article-box">

	<h3 class="title-container">
		<%= link_to "#{article.title}", article_path(article.id) %>
	</h3>
	<div class="entry-image">
		<%= link_to image_tag("#{article.sumnail}", alt:"#{article.title}", size:"400x500"), article_path(article.id) %>
	</div>
	<div class="entry-movie">
		<%= sanitize "#{article.sumnail}" %>
	</div>
	<ul class="entry-data">
		<li class="description">
			<p><%= sanitize "#{article.content}"%></p>
		</li>
		<li class="readmore">
			<%= link_to "[動画を見る]", article_path(article.id) %>
		</li>
		<li class="category">
			<a><%= link_to "#{article.category}", '#' %></a>
		</li>
		<li class="date">
			<%= "#{article.created_at.getlocal}"%>
		</li>
	</ul>
	<ul class="entry-meta">
		<li class="domain">
			<span class="domain">
				<%= link_to "#{article.domain}", "#{article.domain}" %>
			</span>
		</li>
	</ul>
</div>

